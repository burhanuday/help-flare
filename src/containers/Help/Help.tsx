import React from "react";
import Header from "../../components/Header/Header";

const Help: React.FC = (props: any) => {
  return (
    <div>
      <Header />
      help area page
    </div>
  );
};

export default Help;
