import React from "react";
import Header from "../../components/Header/Header";

const Profile: React.FC = (props: any) => {
  return (
    <div>
      <Header />
      Profile page for helper
    </div>
  );
};

export default Profile;
